# -*- encoding: utf-8 -*-
"""
locksmith.plugins.actuary package

Usurance Actuarial role-plugin (HOA #4): a dormant domain-specific surface
that reveals itself only when the vault holds a valid, active, chain-verified
``actuary_role`` ACDC issued by usurance-admin (see
``locksmith.plugins.actuary.plugin.ActuaryPlugin``).
"""
