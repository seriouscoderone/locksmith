# SDD ledger — plan: docs/superpowers/plans/2026-08-08-cuo-mandate-form.md

Branch: claude/cuo-mandate-form (branched from development)
Base (before Task 1): 83a7e93c

Dispatch order is 1, 3, 2, 4, 5, 6, 7, 8 — NOT the document order. Task 2
(validation) imports `mandate_copy`, which Task 3 creates, so validation's tests
cannot run at its own task boundary unless copy lands first. The plan says so in
Task 2's step 4; this is executing that note rather than editing the document.

## Plan defects found during execution (mine, not the implementers')

- **Task 1, NEEDS_CONTEXT (resolved):** the plan's tests call `egf_local_dir()`
  directly, but it returns None until `branding.brand()` has run — it reads
  `_brand_source_dir`, populated by `load_brand()` as a side effect. Nothing in the
  plan activated a brand. Affects Tasks 1, 2, 4, 5, 6.
  Second layer the implementer could not have seen: `src/locksmith/release/*/` is
  GITIGNORED (`.gitignore:244`) — the usurance bundle is generated by
  `scripts/brand_apply.py`, so a fresh clone or CI has no usurance brand at all.
  Resolution: a scoped `tests/plugins/cuo/conftest.py` autouse fixture that BUILDS
  the bundle if absent then activates it, mirroring `tests/conftest.py:15-50`, with
  `_reset_cache_for_tests()` on both sides so the brand cannot leak into later test
  modules. Asserts rather than skips, and carries a test proving it activated
  something so it cannot go vacuous.
- **Task 1 review (Important x2):** (a) mutation test proved `required=True` could be
  hardcoded with 9/9 still passing — every bundle field is required, so the
  `required=False` branch was unexercised; fix round adds a synthetic-schema test.
  (b) `required`-derived order is NOT robust — an alphabetical `required` yields
  alphabetical order with no resistance, so the earlier fix relocated the fragility
  rather than removing it. RESOLVED BY DESIGN: `mandate_copy.FIELD_ORDER` becomes the
  single authority for field sequence (form layout AND error order); plan patched in
  Tasks 2, 3, 6; `MandateSchema.order` demoted to a canary.
- Task 1: complete (commits 9bdf0cf..dfcee2d, review clean — spec ✅, quality
  Approved; 3 findings fixed and each re-verified BY REPRODUCTION: the mutation now
  fails the new synthetic-schema test, a malformed template raises SchemaSourceError
  with the path, and the order docstring no longer overclaims). 10 passed.
  Beyond the brief, by my authorisation: tests/plugins/cuo/conftest.py.
- **Task 3 review (Critical, in-diff):** `_REQUIRED`'s leading underscore made
  `_all_strings()` skip six strings the form renders directly — reviewer injected
  "Please choose…" (the banned word the module exists to police) and all 12 tests
  still passed. Fix round: rename to public `REQUIRED`, plus an explicit test that
  the scan reaches it by name. Also: error_summary rejects non-int; TOKENS
  ratification made bidirectional (it over-ratified `field`/`allowed`);
  required-message test covers all six fields.
  Minor PARKED, no change: JURISDICTION_PATTERN / pattern_error do not echo the
  offending value while three siblings do — the project's own exemplar does not
  echo either, and for jurisdiction we deliberately cannot claim to know whether the
  value is real.
  Adjudicated NOT a defect: `_all_strings()` skipping `__doc__`. Docstrings are never
  rendered; the module's docstring safely quotes "Invalid input." as a counter-example.
- Task 3: complete (commits e889a2a..625fee3, review clean — spec ✅, quality
  Approved after one round; all 4 findings re-verified BY REPRODUCTION, including
  that renaming REQUIRED back to _REQUIRED now fails the new by-name guard).
  25 passed.
  Minor (deferred, for final-review triage): `error_summary(True)` is accepted —
  bool subclasses int — and renders "Fix True error before signing.". No real caller
  can reach it (`len()` never returns a bool); one `isinstance(count, bool)` line
  closes it if the final review wants it.
- **Task 2 review: spec ❌, quality Needs work — TWO CRITICALS, both mine, both
  letting a permanently-signed inverted window through.** (1) `date.fromisoformat`
  accepts `20270101` and `2027-W01-1`; the schema has NO pattern on the date fields;
  so `opens < closes` was a STRING compare that inverts across forms. Measured clean
  pass on a −364-day window, reachable from the shipped free-text UI. (2) `str()`
  coercion let a non-string pass the field check and then skip the gate's own
  isinstance test — `date` objects and ints both validated clean on an inverted
  window. Fix = parse-then-compare via a non-coercing `_as_date`, verified by the
  reviewer at 42/42 before it restored the file.
  Also 3 Important (untested mirror boundary of the interval test; error SEQUENCE
  asserted by nothing since `_fields()` returns a set; `min_length`/`min_items`
  shadowed by `_empty` and dead-tested) and 4 Minor. **7 of 20 mutations survived.**
  Plan patched with Global Constraint 13 so Tasks 4 and 6 cannot re-introduce it.
  Minor PARKED: `import mandate_copy as copy` shadows stdlib `copy` (harmless here;
  renaming in one module while Tasks 3/6 use `copy.` would be worse).
- Task 2: complete (commits 9c7b148..831fc0a, review clean after one round — both
  Criticals CLOSED, all 4 attack vectors now rejected, and every previously-surviving
  mutation reproduced and confirmed caught). 52 passed.
  Notable: the implementer's FIRST attempt at the error-sequence test did not
  discriminate — on the real bundled EGF, FIELD_ORDER and schema.order are identical,
  so it rebuilt the test against a deliberately scrambled `.order`. Self-caught.
  Minor (deferred): `windows_overlap`'s type hints still say `str` though it now
  accepts anything via `_as_date`; `_as_date` parses twice on the overlap path.
- **Task 4, asked before implementing (plan defect, self-contradictory):** the brief's
  `_EMPTY = QDate(1900,1,1)` as `minimumDate` CLAMPS — `setMinimumDate` pulls any
  earlier `setDate` up to the minimum — so the brief's own test date `1752-09-14`
  silently became 1900-01-01, compared equal to the sentinel, and read back as EMPTY.
  The brief's implementation and its own test could not both pass. Verified by direct
  PySide6 probe by the implementer. Resolution: sentinel moved to `QDate(1,1,1)`,
  Qt's earliest, plus a test asserting the RELATIONSHIP (no representable date is
  clamped) rather than one example. Plan patched.
- **Task 4 review (Critical, found by INTERACTIVE probing):** with the field empty and
  focused, one Up-arrow or wheel-scroll silently exits the empty state and yields
  `0001-02-01` — a date the user never chose, on a form whose output is permanent and
  public. `specialValueText` shows a placeholder at the minimum but Qt still treats
  the minimum as a value. Inherited from my brief's reference code (bare QDateEdit +
  specialValueText); survived my writing it AND two rounds of the implementer's
  probing, because all of that probed the sentinel arithmetic and none of it tried
  the keyboard. Fix: `stepBy` no-ops while empty, normal once a date is set; tests
  drive real QTest/QWheelEvent input rather than calling stepBy directly.
  Also Important: nothing locked in "changed does not fire spuriously" (a spurious
  emit would validate before the user acts, breaking no-errors-before-first-submit).
  Minor: the comment claimed year 1 is Qt's earliest date; QDate accepts BCE.
  Second Qt trap confirmed BY ME: QDateEdit's default minimumDate is 1752-09-14, and
  setMinimumDate(QDate(1,1,1)) does NOT lower it — only setDateRange does.
- Task 4: complete (commits 8289465..1cbccfe, review clean after one round — Critical
  CLOSED with real QTest/QWheelEvent reproduction; reviewer independently proved the
  wheel routes through `stepBy` by SPYING on the call rather than inferring from
  before/after values, so the single override is a complete fix, not half of one).
  65 passed.
  Minor (deferred): the guard's completeness depends on QAbstractSpinBox.wheelEvent
  dispatching through stepBy — true on PySide6 6.10.3, worth re-checking on upgrade.
  Typed-digit entry could not be exercised under QT_QPA_PLATFORM=offscreen, isolated
  to a QTest/offscreen limitation (a vanilla QDateEdit behaves identically), not the
  guard; the calendar-popup escape path was confirmed working.
- **Task 5 review (Critical):** the thesis QLabel was left at Qt's default AutoText,
  so a thesis containing markup renders AS markup with the tag characters invisible
  — measured by sizeHint against a PlainText twin. Not hypothetical: the thesis is
  free prose and "hold loss ratio <60% in Utah" would display as "hold loss ratio "
  while signing the full string. That defeats the dialog's only purpose, which is
  showing exactly what will be signed. Fix: PlainText on the thesis AND defensively
  on the generic value row (currently masked only by other layers' patterns).
  Also Important: two strings hardcoded inline instead of in mandate_copy (authorised
  the implementer to add them there); and the one-shot confirm guard had NO
  regression test — deleting setEnabled(False) left all 72 green.
  DEFERRED RISK, recorded not fixed: the one-shot guard is UI-only. Real protection
  against a duplicate immutable mandate is anchor-side idempotency, which lives in
  keri_serviceaid and is outside this plan's scope. The implementer raised this
  unprompted and it is the right instinct.
- Task 5: complete (commits 7e18ab3..909ce6b, review clean after one round — Critical
  CLOSED: setTextFormat(PlainText) verified present on BOTH the thesis and the generic
  row value; both mutations reproduced). 76 passed.
  Minor (deferred, for final-review triage): the plain-vs-rich proof is a sizeHint
  comparison against a PlainText twin — robust and it discriminated in both live
  mutations, but indirect. A direct `label.textFormat() == Qt.TextFormat.PlainText`
  assertion would pin the configuration as well as the behaviour.
  Minor (deferred): REVIEW_IN_FORCE_LABEL / REVIEW_THESIS_LABEL were added to
  mandate_copy without going through the three-draft brand-voice process, on the
  grounds that they are structural labels rather than reader-facing prose.
- **Task 6 implemented** (commits dbc14e6..378d8ca, 3 commits so each finding reverts
  independently). 98 passed in tests/plugins/cuo/; tests/plugins/ went 251+2 failed ->
  254 passed/1 skipped (it FIXED two pre-existing failures with a roles conftest);
  tests/ui + tests/unit 535 passed; ruff clean. Three-lens review + adversarial
  verification dispatched.
  **Eight findings, FOUR of them defects in my brief:**
  1. Critical — `insertItem(0, placeholder)` made combo count 9 while the brief's own
     test demanded 8. `setPlaceholderText()` before `addItems()` gives 8 and leaves
     currentIndex at -1 by itself. (Probed.)
  2. Critical — the one-shot guard CANNOT live in the dialog: its guard is a disabled
     BUTTON, and the brief's test calls `_on_confirm()` directly twice, which
     re-emits. Guard moved to the page, where the irreversible act happens.
  3. High — the brief's fixture used `return p`, so the parent was collected mid-test
     and all 11 tests errored in teardown ("Internal C++ object already deleted").
     `yield p` fixes it.
  4. High — **a comma does NOT commit a token**: LocksmithTextListWidget has no comma
     handling at all, and devctl's `type` cannot even reach the container. My brief
     asserted the opposite and Task 8's helper depends on it. Fixed inside
     plugins/cuo/ (objectName on the inner input, live comma-commit, pending token
     included in the payload, flush on submit); `type "BI,PD"` verified end to end.
     **Also resolves Task 8's flagged QDateEdit risk** — .windowOpens/.windowCloses
     name the QDateEdit's internal QLineEdit and writing it really moves the date, so
     Task 8's helper works as written with no devctl change.
  5. High (its own) — its comments contained a banned regex and an enum literal;
     Task 7's guards read WHOLE FILES, so they would have failed. Caught before
     handing over. Worth knowing: the literal guard catches comments and docstrings.
  6. High (its own) — `set_field("coverages", ...)` appended instead of replacing, so
     reset_form() cleared nothing. Found by hand-driving, not by the suite.
  7. High (its own) — **98 GREEN TESTS OVER A VISIBLY BROKEN FORM.** Wrapped labels
     report a one-line MINIMUM, so every group was squeezed ~40px and controls painted
     over the help text (14px on jurisdiction, 37px on thesis) at EVERY window size.
     Found by rendering the page and reading the screenshots. This is
     feedback_watch_the_ui_yourself, rediscovered independently.
  8. Two pre-existing roles tests built the page with no brand active and began
     failing (the page reads its EGF at construction, loudly, by design).
  **Concerns to triage:** declared/failure banner strings are inline, not in
  mandate_copy; REVIEW_SIGNER's {cuo_name} has no source (cuo_role carries protocol
  fields only) so it falls back to the identifier alias; the read-back shows raw enum
  tokens (workers_compensation) because the combo's item text must stay the enum value
  for Task 8's `select value="auto"` — a derived `value.replace("_"," ").capitalize()`
  display would fix the UX without adding a literal, and Task 8 would select by the
  humanized text; Cancel's clear-the-form behaviour is invented; and the shared `page`
  fixture NEVER lays the page out, so geometry assertions on it are vacuous.

## Task 8 pre-flight (checked while Task 6's review ran, no repo writes)
- devctl 0.2.0 IS what a spawned test wallet loads (~/.locksmith/plugins/ui_tester
  at 05c0af1). `condition == "enabled"` exists (server.py:768) and `click` refuses a
  disabled target with an explanatory error. Task 8's helper works as written.
- The arc spawns a VANILLA admin plus a re-rooted TEST brand, not the shipped
  usurance EGF ("a branded HOA trusts exactly the authority ... against that
  brand"). `build_test_brand` copytrees ugard's EGF, `_reroot_corpus` re-roots and
  re-SAIDs the templates, and the EGF is re-derived so micro_apps[] points at the new
  SAIDs. `schema_source` resolves by role_id, never by a hardcoded SAID, and
  `_reroot_corpus` rewrites only authz.issuer — so payload_schema survives. No change
  needed.
- **STALE COMMENT to fix in Task 8:** testegf.py:78-82 says "ugard's holds eight" and
  overlays the three credential schemas from brands/usurance/egf/. This morning's EGF
  publish fix means ugard's copy now holds all ten, so that loop is a no-op (guarded
  by `if target.exists(): continue`). Harmless code, false comment — the same
  doc-drift pattern that bit us twice today.
- Task 6: complete (dbc14e6..2769b2e; 3-lens review + adversarial verifier, 32/33
  findings held, then one fix round). **137 passed** (98 -> 137); tests/plugins/ 293
  passed/1 skipped; 21 mutations run, 19 killed, 2 proven equivalent.
  Review's own headline finds: (a) you could not TYPE a date into an empty field —
  specialValueText leaves the placeholder IN the line edit and digits append to it;
  invisible to 98 tests because all of them drive it with setText. (b) One Enter on
  the unread read-back signed. (c) The read-back's ONE job — showing what will be
  signed — was asserted by nothing; a divergent payload left the suite green.
  13th defect, exposed by fixing the objectName hole: scroll-to-first-invalid-field
  had NEVER worked (ensureWidgetVisible ran before error labels grew the column).
  Implementer correctly refused 3 of my instructions, with measurements: my
  keyPressEvent recipe swallowed keystrokes (the VALIDATOR is the gate, not the
  placeholder — needed setSelectedSection(0)); O5 was redundant source not a test
  hole; 2 mutations are genuinely equivalent (QLabel.sizeHint clamps to maximumWidth
  under wordWrap), ruled out a 640-is-Qt-default coincidence by parametrizing at 480.
  DEFERRED / owner rulings wanted:
  * LocksmithDialog base-class fix still owed — ANY dialog in the app that rejects
    leaves the same dangling _current_dialog. Two lines, shared file, outside plan.
  * A stray digit now seeds TODAY's date, so a partially-typed date reads as chosen.
    The ISO read-back is the mitigation. Owner ruling wanted.
  * Return now activates "Keep editing" — safe default, but a behaviour change.
  * COVERAGE_DUPLICATE unreachable from this form (LocksmithTextListWidget keys items
    by text, one layer below the page).
  * The shared `page` fixture lays out nothing; geometry tests must build their own shell.
  * Spinner (§4.3/§3.2 ask for one; "spinner" appears nowhere in the plan) and the
    asterisk tooltip (spec says generic, impl uses the specific required message —
    an improvement) are SPEC amendments, not code changes.
- Task 7: complete (9629602). 143 passed (+6). Both guards observed FAILING then
  passing: pasting `_TEMP = "workers_compensation"` fails guard 1 naming file+literal;
  corrupting the minItems comparison fails guard 2 on line_of_business.
  Guard 1 redesigned as TWO checks: a strict quoted-literal scan (comments included —
  a quoted restatement in a comment is still drift) and a \b-bounded bare-word scan
  restricted to real code via `tokenize` blanking, so prose like "lifecycle" or
  "any property the schema" cannot trip it.
  Honest note: the brief's original substring code ACTUALLY PASSED on the current tree
  (it dodges the false positive by quote-wrapping), so this hardened a general
  fragility rather than fixing a live break. And the bare-word check has no historical
  defect case — the implementer could not construct a case where it fires and the
  quoted check does not. Cheap defence-in-depth; kept.
- Task 8 helper: complete (54250ee). tests/plugins/cuo/ still 143 passed;
  test_actuary_observes_and_attests_via_ui passed 1/1 in 19.31s VISIBLY, driving the
  rewritten helper end to end including the review-dialog confirm.
  **Another plan defect it caught:** my replacement snippet DROPPED the final
  `wait_for cuoMandatePage.declaredBanner`. The confirm click only SCHEDULES the
  anchor (ServiceaidIssueDoer) and returns synchronously, so the helper would return
  before the mandate existed. Two of three callers mask it by polling a slower
  downstream effect; test_cuo_mandate_via_ui.py does NOT — it would have stayed green
  even if issuance broke entirely. Restored (same objectName, same 10s timeout as the
  old helper).
  Also corrected the stale testegf.py comment, with the counts verified independently.
  Arc run started by me, visibly, for the owner to watch.

## DEFERRED FINDINGS — the whole-branch review must triage each of these

Nothing below was fixed. Each was found, judged, and consciously left. The final
review decides which must close before this merges to `development`.

**Owner rulings wanted (behaviour, not defects):**
1. A stray digit in an empty date field now seeds TODAY's date, so a partially-typed
   date reads as chosen. The ISO read-back is the only mitigation. (Task 6)
2. `Return` now activates "Keep editing" rather than the primary — deliberately safe,
   but a behaviour change from every other dialog in the app. (Task 6)
3. The read-back displays the raw enum token `workers_compensation`, because the
   combo's item text must stay the literal enum value for devctl `select value="auto"`.
   A derived `value.replace("_"," ").capitalize()` display would fix the UX with no
   literal added, and Task 8's helper would select by the humanised text. (Task 6)
4. Cancel's "clear the form" behaviour is the implementer's invention; the spec only
   asked the button exist. (Task 6)

**Owed elsewhere (outside this plan's scope):**
5. `LocksmithDialog` base class: `reject()`/`accept()` never clear `_current_dialog`,
   so ANY dialog in the app that rejects leaves a dangling class attribute and every
   later dialog show raises. Two lines, shared file. Worked around locally by using
   `close()`. **Deserves its own backlog item.**
6. Duplicate-mandate protection is UI-only. Real protection is anchor-side idempotency
   in `keri_serviceaid`. There is no correcting a mandate, only declaring another.
7. `_cuo_hab`, `_ensure_mandate_schema_pinned`, `_anchor`, `_retire_listener` are
   executed by NO test (83% coverage, those four in the miss list) — every test that
   reaches confirmation replaces `page._anchor` with a lambda. The brief declared this
   machinery out of scope, but its own docstring warns a listener on the wrong source
   name "receives nothing, silently".
8. `PRODUCT_MANDATE_SCHEMA_SAID` is duplicated in cuo/page.py, actuary/page.py and
   product_designer/page.py with nothing tying any of them to the bundle. Changing one
   character leaves the suite green.

**Test-quality residue:**
9. Two mutations are genuinely equivalent (`setFixedWidth`→`setMaximumWidth` on groups
   and labels) because `QLabel.sizeHint()` clamps to `maximumWidth` under `wordWrap`.
10. The geometry gate's headline overlap assertion is inert — the squeeze check fires
    first on field 1, so a change producing overlap WITHOUT squeezing would pass.
11. The shared `page` fixture lays out nothing, so any geometry assertion written on it
    is vacuous. The committed geometry test builds its own shell and says so.
12. The bare-word half of the schema-literal guard has no historical defect case; the
    implementer could not construct one where it fires and the quoted check does not.
13. `COVERAGE_DUPLICATE` is unreachable from this form: `LocksmithTextListWidget` keys
    items by text one layer below the page. Validation still catches a duplicate typed
    but uncommitted.
14. `error_summary(True)` is accepted (bool subclasses int) and renders "Fix True
    error before signing." No caller can reach it; one isinstance line closes it.
15. `windows_overlap`'s type hints still say `str` though it accepts anything via
    `_as_date`; `_as_date` parses twice on the overlap path.

**Spec amendments, not code changes:**
16. §4.3/§3.2 ask for a `WaitingSpinner` in the Anchoring state; the word "spinner"
    appears nowhere in the plan and none was built.
17. §3.1 specifies a generic "This field is required." tooltip on the asterisk; the
    implementation uses the specific per-field required message. The deviation is an
    improvement — amend the spec.
18. `mandate_copy`'s two structural labels (`REVIEW_IN_FORCE_LABEL`,
    `REVIEW_THESIS_LABEL`) skipped the three-draft brand-voice process.

## WHOLE-BRANCH REVIEW (opus, 21 commits / 4220 insertions) — verdict: NOT YET MERGEABLE

Acceptance is real: 143 unit / 293 in tests/plugins / the four-window arc passed
TWICE (154.67s, then 153.84s truly visible) / actuary UI 1/1 in 19.31s.

### Three merge blockers, in order

1. **CRITICAL — one `Escape` bricks the mandate form for the session.** I routed only
   the "Keep editing" BUTTON through `close()`. Escape (QDialog's default) and the
   header X (`dialogs.py:268` -> `self.reject()`, and the dialog takes the default
   `show_close_button=True`) both go through `reject()`, which never clears
   `LocksmithDialog._current_dialog`. Measured: submit -> Escape -> submit raises
   "Internal C++ object (MandateReviewDialog) already deleted", and every other dialog
   in the process is dead too. `close()` is the ONLY safe exit of the three.
   Root cause PRE-EXISTS on development (WA_DeleteOnClose landed a55f22f8, 42
   accept()/reject() sites across 12 files) — the branch's own defect is that the
   workaround comment CLAIMS the hole is closed while two routes stay open, on the one
   irreversible screen in the app. Also: `_on_confirm` disables both buttons but not
   the X, so the modal that must survive to receive the outcome can still be destroyed
   mid-flight.
   FIX: clear `_current_dialog` in `LocksmithDialog.reject()`/`accept()`
   (dialogs.py:544-552, two lines) + `show_close_button=False` at review_dialog.py:117
   + a test that Escape leaves it clear and a second submit still opens.
   **Do NOT override reject() to call close()** — `QDialog::closeEvent` calls
   `reject()`, so that recurses.

2. **IMPORTANT — the overlap error names a remedy that cannot work.** `existing_mandates()`
   (page.py:769-776) enumerates `reger.schms` and never consults the TEL, so a WITHDRAWN
   mandate still blocks — while WINDOW_OVERLAP says "Withdraw that mandate or set this
   window to start after it closes." Only moving the window works. `actuary/page.py:472-474`
   already does this correctly (`tevers.get(regid).vcState(said)`), as does
   `plugins/manager.py:480-499`; the CUO page is the only one ignoring it.

3. **IMPORTANT — a mistyped SAID makes the overlap gate SILENTLY vacuous.** page.py:117
   holds the pin, :770 uses it, :777 swallows everything into `logger.debug`. And
   page.py:115 CLAIMS `tests/plugins/roles/test_pin_regression.py` verifies it — that
   file checks only the three ROLE SAIDs. The one test touching the registry read fakes
   `schms.get=lambda keys=None: [...]`, ignoring `keys`, so it passes for any pin.
   FIX: add the mandate SAID to `test_schema_pins_match_egf_catalog`, log the
   empty-registry case at warning, and delete the false provenance comment.

### TWO LEDGER ENTRIES ABOVE ARE WRONG — corrected here

- **Deferred #7 ("the signing path is executed by NO test") is WRONG.** That was a UNIT
  coverage reading. The four-window arc runs all four end to end: confirm ->
  `_confirm_review` -> `_anchor` -> `_ensure_mandate_schema_pinned` ->
  ServiceaidIssueDoer -> credential_issued -> `_retire_listener` -> `_show_declared` ->
  declaredBanner. `_cuo_hab` runs twice. The real gap is a UNIT-level net for the
  legacy-source-name trap. Backlog, not a blocker.
- **Deferred #13 ("COVERAGE_DUPLICATE unreachable") is WRONG.** Measured reachable:
  chips `bi` + `BI` are two items (the list keys by text), `read_tokens` canonicalizes
  both to `BI`, and submit reports "BI is listed twice." — naming a code that appears
  ONCE on screen. Do not retire the rule; add the test.

### Further review findings not previously logged
- `MandateReviewDialog.fail()` is DEAD CODE — no src caller; two tests exercise a state
  production cannot reach and read as failure-path coverage.
- A lowercase coverage is silently upper-cased, not rejected; spec §6 says rejected.
  Behaviour is defensible (matches jurisdiction; the read-back is honest) -> amend spec.
- `date_field.set_iso("20270101")` silently EMPTIES the field while `validation._as_date`
  accepts it: the two modules disagree on what a `format: date` value is.
- `page.py:134-185` (`_pattern_prefix`/`_pattern_case`/`_canonical`) is a hand-rolled
  regex parser — schema interpretation in a THIRD module — with no direct test. Degrades
  safely on the four patterns probed, but a changed EGF pattern changes canonicalization
  with nothing watching.
- `blur_field` classifies "is this required?" by comparing RENDERED STRINGS; `FieldError`
  carries no kind. Harmless only while min_length/min_items stay shadowed by `_empty`.
- `schema_source` consumes a fixed keyword set: `maxLength`, `maxItems`, `const`,
  `minimum` have no slot and no test asserts coverage — a tightened EGF would be
  enforced only by the issuer, which is the loop this branch exists to close.
- `_anchor` captures `signals` locally while `_retire_listener` re-reads
  `self._app.vault.signals`; a vault swap between them disconnects the wrong object.
  Nothing disconnects `_pending_listener` on page teardown.
- `error_summary(len(errors))` counts ERRORS not FIELDS, so "Fix 7 errors" can appear
  with 6 fields marked.
- LAYERING: substantially holds — no schema constraint is duplicated anywhere. Three
  leaks: `existing_mandates()` owns half the overlap rule (the wrong half), pattern
  interpretation lives in a third module, and `date_field` fixes what a date IS.
- COLLATERAL: none. tests/plugins/roles 24 passed/1 skipped; no stale `effectiveWindow`
  selector anywhere; sitecustomize's `_show_declared` wrapper still lines up.

## BLOCKERS CLOSED 2026-08-08 — commits 282fbf96, 838ed129, 2afd05f1

All three fixed inline (no SDD dispatch — the review had already named the exact
change for each). Every claim below was measured, not reasoned: **14 of the new
tests fail against the pre-fix source with `__pycache__` cleared, 0 after.**

**1. `_current_dialog` — CLOSED (282fbf96).** Probed the base class first, on Qt
6.10.3:

    close   -> _current_dialog None,  closeEvent ran
    reject  -> set, valid=False,      closeEvent did NOT run
    accept  -> set, valid=False,      closeEvent did NOT run
    Escape  -> same as reject

`_release_current_dialog()` now runs from `closeEvent`, `reject` AND `accept`;
`showEvent` discards a dead pointer via `Shiboken.isValid` instead of
dereferencing it (measured: `!=` against a deleted object does NOT raise, so the
guard is safe). Read-back takes `show_close_button=False` and refuses `reject()`
while an anchor is in flight — verified `close()` returns False there too, since
`QDialog::closeEvent` routes through `reject()`. Not implemented by routing
`reject` through `close` (that recurses).

*The 42-site audit the backlog item asked for is DONE and clean:* all seven
`closeEvent` overrides in `src/` also connect `QDialog.finished`, which fires on
reject/accept, so `_current_dialog` was the only `closeEvent`-only cleanup in the
app. Nothing else to fix. The backlog item can be closed with the fix, not
reopened as its own project.

**2. Overlap remedy — CLOSED (838ed129).** Filtered, not re-worded: the spec keeps
withdrawal "named without a route" (design §5.1, owner declined the route twice),
so trimming the copy would have contradicted an owner ruling. Grounded in the
vendored ACDC v1.1 spec rather than convention — for the indirect case the ACDC's
states "are either *issued* or *revoked*", and § *Transaction state, `ts` field*
fixes that set at two values. The four ilks are keripy's, not the spec's (spec:
`ts` + `rip`; keripy: `et` + `vcp`/`vrt`) and that is now said out loud in the
constant's comment. keripy's `vcstate` REFUSES any ilk outside
`iss`/`bis`/`rev`/`brv` — a test drives it with all four plus three rejects, so
"not revoked" is sound by construction and a keripy change fails here.

**3. Vacuous overlap gate — CLOSED (2afd05f1).** The review's instruction ("add it
to `test_schema_pins_match_egf_catalog`") could not be followed: measured, that
test resolves `egf["credentials"]` by id and the catalog holds exactly
cuo_role/actuary_role/product_designer_role. The mandate is declared by the CUO
template as what `declare_product_mandate` mints. Two new tests walk the real
chain (EGF doc -> cuo micro-app -> minting command -> exported credential) and
compute the bundled schema's SAID with `Schemer`; both catch a one-character pin
mutation. Also fixed the fake that ignored `keys` — it now discriminates, with a
self-test proving it.

### Method note worth keeping

Two gates were vacuous on their first pass and only measurement found it:

- `test_a_dialog_still_opens_after_the_previous_one_exited` PASSED against the bug
  on three of four exits, because `WA_DeleteOnClose` POSTS the deletion and the
  corpse was still valid; it now waits for the delete.
- Restoring a mutated `page.py` wrote an identical-SIZE file inside the same mtime
  second, so Python reused the `.pyc` from the mutated source and two tests
  "failed" against correct code. **Clear `__pycache__` between mutation runs** —
  a same-size edit defeats mtime+size invalidation.

### Still open (not blockers)

The four owner rulings and the 18 triaged deferred findings above stand unchanged.
